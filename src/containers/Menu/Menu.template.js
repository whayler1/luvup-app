import React from 'react';
import {
  Text,
  View,
} from 'react-native';

import { scene } from '../../styles';

export default () => (
  <View style={scene.container}>
    <Text>the menu</Text>
  </View>
);
