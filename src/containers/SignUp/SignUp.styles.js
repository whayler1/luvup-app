import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  errorWellWrapper: {
    marginTop: 32,
    marginBottom: 0,
  },
  submitWrap: {
    flex: 1,
  },
});

export default styles;
