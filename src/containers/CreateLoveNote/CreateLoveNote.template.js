import React, { PureComponent } from 'react';
import {
  View,
  Text,
  Animated,
} from 'react-native';

import styles from './CreateLoveNote.styles.js';
import { forms, buttons, scene, modal, vars, } from '../../styles';

export default () => (
  <Animated.View>
    <Text>Create Love Note</Text>
  </Animated.View>
);
