import { StyleSheet } from 'react-native';

const radius = 2;

export default StyleSheet.create({
  infoContainer: {
    // width: 200,
    alignSelf: 'stretch',
  },
  infoButton: {
    backgroundColor: 'lightblue',
    borderRadius: radius,
  },
  infoText: {
    textAlign: 'center',
  },
});
