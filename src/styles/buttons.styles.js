import { StyleSheet } from 'react-native';

const radius = 2;

export default StyleSheet.create({
  infoButton: {
    backgroundColor: 'lightblue',
    borderRadius: radius,
    marginTop: 30,
  }
});
